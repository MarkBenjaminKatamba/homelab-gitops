# This file helps to dynamically override the image tag for the taskflow-backend deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: taskflow-backend
spec:
  selector:
    matchLabels:
      app: taskflow-backend
  template:
    metadata:
      labels:
        app: taskflow-backend
    spec:
      containers:
        - name: taskflow-backend
          image: ghcr.io/markbenjaminkatamba/taskflow-backend-dev:main-1a8cb64c542358b246692b348e4cbc8d4cc8c051


